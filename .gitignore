# Swift
native/.build/
native/build/
*.xcuserstate
.swiftpm/

# Generated icon artifacts — build.sh redraws these from icon/make-icon.swift
native/icon/AppIcon.iconset/
native/icon/AppIcon.icns

# Python prototype
.venv/
__pycache__/
*.py[cod]

# Local calibration data, not source
model.json
state.json

# Signing and notarisation secrets — never commit these
*.p8
*.p12
*.cer
*.certSigningRequest
*.provisionprofile

# macOS
.DS_Store

# Release artifacts
native/dist/
