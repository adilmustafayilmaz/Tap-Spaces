# Swift
.build/
build/
*.xcuserstate
.swiftpm/

# Generated icon artifacts — build.sh redraws these from icon/make-icon.swift
icon/AppIcon.iconset/
icon/AppIcon.icns

# Local calibration data, not source
model.json
state.json

# Signing and notarisation secrets — never commit these
*.p8
*.p12
*.cer
*.certSigningRequest
*.provisionprofile

# macOS
.DS_Store

# Release artifacts
dist/
